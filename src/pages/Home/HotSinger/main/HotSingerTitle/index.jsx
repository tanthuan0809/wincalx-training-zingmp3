// libs import
import React from "react";

// components

// style
import "./style.scss";

/**
 * HotSingerTitle - description a title of hot singer component
 * @param { string } title // title of hot singer component
 */

const HotSingerTitle = ({ title }) => {
  return <div className="hot-singer-title-wrapper">{title}</div>;
};

export default HotSingerTitle;
